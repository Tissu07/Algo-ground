#include<bits/stdc++.h>
using namespace std;

int main() {
    string str;
    

    string s1, s2;  // only one word in one variable before space
    cin >> s1 >> s2;
    cout << s1;


    getline(cin, str); // for one line input includen space
    cout << str;

    string s = "hello";
    cout << s[2];
    
     return 0;
}